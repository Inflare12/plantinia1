import {redirect} from 'next/navigation';import {supabaseServer} from '@/lib/supabase-server';export default async function Account(){const s=await supabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:p}=await s.from('profiles').select('display_name,plan,diagnoses_used').eq('id',user.id).single();return <main className="auth"><h1>Your account</h1><section className="card"><p><b>Email:</b> {user.email}</p><p><b>Plan:</b> {(p?.plan||'free').toUpperCase()}</p><p><b>Diagnoses used:</b> {p?.diagnoses_used||0}</p><a className="btn" href="/pricing">Manage plan</a></section></main>}